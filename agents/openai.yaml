interface:
  display_name: "Visual Explainer Cards"
  short_description: "Plan storyboards, then create illustrated social cards"
  default_prompt: "Use $xuanqing-visual-explainer-cards to plan and validate storyboard.yaml first, then turn this topic into a Xiaohongshu card series with varied content-page layouts and one or more independently planned model-generated illustrations on every non-cover card."
