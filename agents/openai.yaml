interface:
  display_name: "Visual Explainer Cards"
  short_description: "Create validated visual explainers with built-in or API images"
  default_prompt: "Use $xuanqing-visual-explainer-cards to turn this topic into a content-driven, illustrated Xiaohongshu card series, then render and validate the final cards."
